# Swift / SwiftPM
.build/
.swiftpm/
DerivedData/
.dd/

# Xcode project is generated — run `xcodegen generate` (source: project.yml)
Reflect.xcodeproj/

# macOS
.DS_Store

# Whisper models (large binaries, downloaded at runtime)
*.ggml
*.gguf
